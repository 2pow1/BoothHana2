# BoothHana2 프로토타입 역기획 및 불일치 분석

## 문서 상태

- 분석 단계: 1단계 — 역기획 및 불일치 분석
- 작성일: 2026-08-09
- 구현 여부: 구현하지 않음
- 분석 대상: [GitHub `prototype`](https://github.com/dkskrn2/booth/tree/main/prototype)의 HTML/CSS/브라우저 렌더링
- 비교 자료: `안건정리_20260801_1653 .txt`, `부스플랫폼_기능명세_초안.xlsx`, 승인된 Lean CRUD 계획
- 기준 우선순위: 최신 안건 정리 → 승인 계획 → 엑셀 초안 → 프로토타입 화면

## 근거 표기

- **[관찰]** 프로토타입 HTML, CSS 또는 브라우저로 렌더링한 화면에서 직접 확인한 내용
- **[명세]** 최신 안건 정리 또는 승인된 계획에서 확정된 내용
- **[명세-초안]** 엑셀 초안에 적혀 있으나 최신 합의로 변경되거나 제외될 수 있는 내용
- **[추정]** 화면과 명세를 연결하기 위해 에이전트가 예상한 구현안. 아직 확정된 요구사항이 아님

별도의 이미지 파일로 된 스크린샷은 제공되지 않았으므로, 이 문서에서 말하는 화면 관찰은 정적 HTML을 실제 브라우저로 렌더링한 결과를 뜻한다.

## 분석 요약

- **[관찰]** 프로토타입에는 총 73개 HTML이 있다. 루트 3개, 팬 16개, 크리에이터 27개, 관리자 18개, 공통 9개다.
- **[관찰]** 화면 지도 표기는 팬 18개, 크리에이터 27개, 관리자 17개, 공통 10개로 되어 있어 실제 파일 수와 일치하지 않는다.
- **[관찰]** HTML 간 로컬 링크의 누락 파일은 발견되지 않았다.
- **[관찰]** 총 125개 버튼, 87개 입력창, 57개 선택창, 12개 텍스트 영역, 43개 표가 있으나 실제 서버 통신이나 저장은 없다. 화면은 대부분 샘플 데이터와 제한적인 프런트 동작만 가진 정적 프로토타입이다.
- **[명세]** 구현 대상은 프로토타입 전체가 아니라 승인 계획의 포함 범위뿐이다.
- **[명세]** 온라인 결제·정산·통계·신고·관심·견적·통합 검색·엑셀 업로드·관리자 사용자 관리는 제외한다.
- **[명세]** React 화면, Java API, Supabase PostgreSQL, Cloudflare R2를 사용하며 카카오 OAuth와 지정 카카오 계정 관리자 규칙을 적용한다.

## 1. 화면에서 확인되는 UI 요소

### 1.1 전체 공통

- **[관찰]** 상단 브랜드 영역, 데스크톱 내비게이션, 역할별 사이드바, 모바일 역할 메뉴, 본문 컨테이너, 하단 서비스 링크가 반복된다.
- **[관찰]** 큰 제목에는 세리프 계열, 본문에는 산세리프 계열을 사용한다. 흰색·아이보리 바탕, 짙은 갈색 텍스트, 코럴·청록·금색 상태색을 사용하는 편집숍/매거진 톤이다.
- **[관찰]** 카드, 패널, 탭, 상태 칩, 기본/보조/약한 버튼, 검색창, 선택 필터, 표, 상세 정보 목록, 빈 상태가 반복된다.
- **[관찰]** 정적 샘플 화면에는 로딩 상태나 실제 API 오류 상태가 거의 없으며 저장 성공·실패 피드백도 실질적으로 동작하지 않는다.

### 1.2 팬 화면

| 화면 | 확인되는 요소 |
| --- | --- |
| 팬 홈 `fan-home` | **[관찰]** 행사·크리에이터·굿즈·마이페이지 내비게이션, 히어로, 통합 검색, 추천 굿즈, 진행 중/예정 행사, 인기 크리에이터, 요약 수치 |
| 행사 탐색 `f-005` | **[관찰]** 행사 검색, 상태·기간 성격의 필터, 행사 카드 목록 |
| 행사 상세 `f-010` | **[관찰]** 행사명·기간·장소, 참가 부스 검색, 카테고리 탭과 선택창, 참가 부스 목록 |
| 부스 탐색 `f-001` | **[관찰]** 부스 검색, 예약 상태 필터, 전체·예약 가능·진행 중·관심 탭, 부스 카드 |
| 부스 상세 `f-009` | **[관찰]** 부스 대표 영역, 소개·위치·SNS, 굿즈 검색, 전체·예약 가능·현장 판매·SOLD OUT 탭, 상품 카드 |
| 상품 상세 `f-002` | **[관찰]** 상품명·가격·상태, 이미지 영역, 설명, 예약/수령 확인 항목, 같은 부스의 굿즈 |
| 예약 요청 `f-003` | **[관찰]** 예약자 정보, 수령일/시간 성격의 입력, 수량, 요청 메모, 선택 상품 요약, 예약 요청 버튼 |
| 내 예약 `f-004` | **[관찰]** 예약 목록과 “예약한 굿즈가 없습니다” 빈 상태 |
| 예약 상세 `f-006` | **[관찰]** 예약번호, 예약·수령 정보, 예약 상품, QR 표시 영역 |
| 기타 팬 화면 | **[관찰]** 신고, 관심 크리에이터, 마이페이지, 프로필/계정, 신고·문의 내역, 크리에이터 목록·상세, 전체 굿즈 탐색 화면이 있다. 이 중 상당수는 현재 승인 범위 밖이다. |

### 1.3 크리에이터 화면

| 화면 | 확인되는 요소 |
| --- | --- |
| 홈 `cr-001` | **[관찰]** 크리에이터 사이드바, 요약 카드, 최근 부스, 주요 관리 화면 바로가기 |
| 행사 목록·상세 `cr-002`, `cr-022` | **[관찰]** 참가 가능한 행사 목록, 행사 정보, 참가 신청 진입 |
| 부스 신청 `cr-003` | **[관찰]** 부스명·코드·SNS 성격의 텍스트 입력, 소개, 이미지 파일 선택, 공개 버튼 |
| 내 부스·부스 관리 `cr-004`, `cr-005` | **[관찰]** 부스 카드, 기본 정보 수정, 이미지 선택, 공개 상태, 상품·공지·공개 페이지 진입 |
| 상품·재고 `cr-006` | **[관찰]** 상품 검색, 캐릭터·판매 유형 필터, 상품 표, 현재 재고·예약 묶임·가용 재고, 숨김·예약 마감·재입고 버튼 |
| 상품 등록 `cr-023` | **[관찰]** 상품명, 캐릭터, 분류, 가격·재고 성격의 입력, 설명, 이미지, 공개·예약 관련 선택값, 저장 버튼 |
| 예약 관리 `cr-008` | **[관찰]** 예약번호·상품·부스 검색, 행사·상태 필터, 예약 표, 빈 상태, 확정 버튼 |
| 예약 수령 `cr-018` | **[관찰]** 예약번호 검색, 예약 결과 표, 수령 완료 확인·처리 버튼 |
| POS `cr-010`, `cr-012`, `cr-025`, `cr-026` | **[관찰]** 상품 버튼, 판매 카트, 수량·금액, 결제수단 성격의 입력, 판매 확정, 판매 목록·상세·취소 |
| 부스 공지 `cr-011` | **[관찰]** 공지 등록·수정 폼, 공지 유형·상태·고정 선택, 공지 표, 저장 버튼 |
| 기타 크리에이터 화면 | **[관찰]** 엑셀 업로드, 행사 당일 대시보드, 견적 요청, 프로필, 공개 URL/QR, 예약 설정, 재고 조정 이력, 상품 일괄 상태, POS 요약·배치 설정 등이 있다. 일부는 승인 범위 밖이다. |

### 1.4 관리자 화면

| 화면 | 확인되는 요소 |
| --- | --- |
| 관리자 홈 `a-001` | **[관찰]** 관리 바로가기, 오늘 항목, 우선 처리 항목, 최근 행사·부스 요약 |
| 행사 목록 `a-002` | **[관찰]** 행사명·장소 검색, 공개·행사 상태·기간 필터, 행사 표, 등록·상세 진입 |
| 행사 등록/수정 `a-003` | **[관찰]** 행사명, 상태, 시작·종료일, 운영 시간, 참가 등록 기간, 장소, 설명, 이미지, 노출 설정, 저장 검증 표, 비공개 버튼 |
| 참가 부스 `a-004` | **[관찰]** 행사 선택, 부스·크리에이터·상품 검색, 공개 상태 필터, 참가 부스 표 |
| 부스 상세 `a-005` | **[관찰]** 부스 정보, 숨김·제거 성격의 관리 동작 |
| 행사 상세 `a-014` | **[관찰]** 행사 상세, 운영 바로가기, 부스·신고 요약, 참가업체 상태 표 |
| 관리자 로그인 `a-015` | **[관찰]** 별도의 관리자 로그인 화면이 존재한다. |
| 기타 관리자 화면 | **[관찰]** 견적 리드, 크리에이터·팬 관리, 신고 처리, 서비스 문의·공지 관리가 있다. 현재 승인된 최소 관리자 범위와 다르다. |

### 1.5 공통·인증 화면

- **[관찰]** `login`과 `c-001`은 팬/크리에이터 이용 유형 선택을 먼저 보여준다.
- **[관찰]** 팬과 크리에이터용 로그인/회원가입 화면 `c-006`, `c-007`, 약관 동의 `c-003`, 권한 없음·세션 만료 `c-008`이 있다.
- **[관찰]** 서비스 문의 `c-002`, 통합 검색 `c-009`, 서비스 공지·점검 `c-010`도 있다.

## 2. 명세서에 정의된 기능

### 2.1 현재 구현 대상으로 확정된 기능

- **[명세]** 팬·크리에이터 카카오 OAuth 로그인, 지정 카카오 계정 ID에 대한 관리자 역할 부여
- **[명세]** 관리자 행사 CRUD, 공개·종료, 행사 참가 신청 목록·승인·반려
- **[명세]** 크리에이터 기본 부스 CRUD, 행사 참가 신청, 승인된 행사별 부스 정보 CRUD
- **[명세]** 상품 CRUD, 이전 행사 상품 정보 복사, 행사별 가격·공개·예약 가능 설정
- **[명세]** 단일 통합 재고, 유한·무한 재고, 재고 수량과 독립적인 수동 `SOLD OUT`
- **[명세]** 부스·상품 이미지를 Cloudflare R2에 서버 제어 서명 방식으로 직접 업로드
- **[명세]** 팬의 공개 행사·부스·상품 목록·상세 조회
- **[명세]** 한 부스 안에서 상품과 수량을 선택하는 직접 예약, 예약번호·QR 표시
- **[명세]** 팬의 내 예약 목록·상세·취소
- **[명세]** 크리에이터의 예약 목록·상세·예약번호 검색·수령 완료
- **[명세]** 간단 POS 판매 생성·목록·상세·취소, 현금·계좌이체·기타 결제수단 메모
- **[명세]** 부스 공지 CRUD와 부스당 최대 한 개의 상단 고정 공지
- **[명세]** 종료 행사의 기존 공개 주소 읽기 전용 유지, 신규 예약·POS 차단, 기존 예약 취소 허용

### 2.2 엑셀 초안에는 있으나 최신 합의에서 제외된 기능

- **[명세-초안]** 장바구니, 주문서, 온라인 결제, 결제 실패·환불 상태
- **[명세-초안]** 사전구매 재고와 현장판매 재고의 분리
- **[명세-초안]** 재고 변동 이력과 중복 수령 방지 중심의 강한 정합성
- **[명세-초안]** 상품별·채널별·결제수단별 판매·매출 요약
- **[명세-초안]** 이메일 또는 소셜 가입 선택, 상세 계정 상태
- **[명세]** 위 항목은 최신 안건 정리와 승인 계획에 따라 현재 구현 대상이 아니다.

## 3. 프로토타입 화면과 명세의 불일치

| 항목 | 프로토타입 관찰 | 현재 명세 | 분석 결과 |
| --- | --- | --- | --- |
| 화면 수 | **[관찰]** 화면 지도 표기와 실제 HTML 수가 다름 | **[명세]** 전체 프로토타입 구현 의무 없음 | **[추정]** 화면 번호를 그대로 React 라우트 계약으로 사용하면 안 됨 |
| 로그인 | **[관찰]** 유형 선택, 팬/크리에이터 가입, 약관, 별도 관리자 로그인 | **[명세]** 카카오 OAuth, 지정 카카오 ID만 관리자, 추가 약관 화면 제외 | **[추정]** 공통 카카오 로그인 진입과 로그인 후 역할 라우팅으로 단순화 필요 |
| 결제·주문 | **[관찰]** UI 문구에 “사전구매”, 가격·총액·결제수단이 보임 | **[명세]** 온라인 결제·주문·환불 없음, 예약과 현장 결제 메모만 존재 | **[추정]** 팬 화면의 구매 표현을 예약으로 통일하고 결제 단계는 만들지 않음 |
| 예약 흐름 | **[관찰]** `f-003`은 예약 폼, `f-006`은 예약번호·QR을 표현 | **[명세]** 한 부스 내 직접 예약, 단순 예약·수령 완료·취소 상태 | **[추정]** 이 두 화면은 유지 가능하나 복잡한 주문 상태·판매자 확정 버튼은 제거 필요 |
| 재고 | **[관찰]** 현재 재고·예약 묶임·가용 재고가 동시에 표시되고 수동 조정 이력 화면이 있음 | **[명세]** 통합 재고 하나, 이력·잠금·복잡한 정합성 제외 | **[추정]** 현재 재고 한 값과 유한/무한·SOLD OUT만 노출해야 함 |
| 무한 재고 | **[관찰]** 대표 공개 화면에서 무한 재고 전용 표현을 확인하기 어려움 | **[명세]** 무한 재고는 재고 문구를 숨김 | **[추정]** 상품 카드·상세·POS 모두 같은 표시 규칙 필요 |
| SOLD OUT | **[관찰]** 팬 부스 탭과 일부 상품 상태로 보임 | **[명세]** 수량과 독립적인 판매자 수동 상태 | **[추정]** 수량 0과 SOLD OUT을 같은 값으로 합치면 안 됨 |
| 부스 공지 | **[관찰]** `cr-011` 관리 화면은 있으나 팬 부스 상세의 상단 고정 공지 표현이 명확하지 않음 | **[명세]** CRUD와 한 개의 상단 고정 공지 필수 | **[추정]** `f-009` 상단에 공지 영역을 새로 배치해야 함 |
| 종료 행사 | **[관찰]** 진행 중·예정·종료 상태 문구는 있으나 동일 URL 읽기 전용 전환은 확인 불가 | **[명세]** 종료 후 수정·신규 예약·신규 POS 금지, 공개 조회 유지 | **[추정]** 동일 화면에 종료 배너와 비활성 동작 상태가 필요 |
| 관리자 범위 | **[관찰]** 팬·크리에이터 관리, 신고, 문의, 공지, 견적 리드까지 존재 | **[명세]** 행사와 참가 승인·반려만 관리 | **[추정]** 관리자 내비게이션에서 범위 밖 메뉴 제거 |
| 검색·관심·추천 | **[관찰]** 통합 검색, 관심 크리에이터, 추천·인기 수치, 고급 필터가 다수 존재 | **[명세]** 통합 검색, 관심, 추천, 고급 필터 제외 | **[추정]** 핵심 목록의 단순 탐색만 남기며 검색은 필수 기능으로 간주하지 않음 |
| 견적·업체 연결 | **[관찰]** 크리에이터 견적 요청과 관리자 리드 화면 존재 | **[명세]** 외부 연락으로 대체하고 MVP 제외 | **[추정]** 관련 라우트와 메뉴를 만들지 않음 |
| 엑셀·일괄·POS 설정 | **[관찰]** 엑셀 업로드, 상품 일괄 상태, POS 버튼 배치·요약 화면 존재 | **[명세]** 엑셀, 일괄 관리, POS 배치, 통계 제외 | **[추정]** 개별 상품 CRUD와 간단 POS만 사용 |
| 장애·서비스 공지 | **[관찰]** 정적 문의와 서비스 공지 화면이 있음 | **[명세]** 그로스라인 이메일 전달 전 정적 장애 화면 제외, 관리자 공지 제외 | **[추정]** 현재 구현하지 않고 계획 변경 시 다시 검토 |
| 파일 업로드 | **[관찰]** 일반 `<input type="file">`만 표시 | **[명세]** 서버가 발급한 제한된 권한으로 R2 직접 업로드 | **[추정]** 화면만 같아 보여도 실제 구현에는 업로드 준비·진행·실패 상태가 추가되어야 함 |

## 4. 화면만으로 알 수 없는 사항

- **[관찰]** 정적 HTML에는 실제 인증 토큰, 세션 수명, 역할 판정 방식이 없다.
- **[명세]** 관리자 판정은 지정 카카오 계정 ID로 한다.
- **[추정]** 서버가 Kakao subject와 허용 목록을 비교해 역할을 내려줘야 한다.
- **[관찰]** 입력 필드의 필수 여부, 길이, 허용 파일 형식·크기, 저장 실패 시 메시지는 대부분 알 수 없다.
- **[관찰]** 삭제가 물리 삭제인지, 연결 데이터가 있을 때 차단되는지 알 수 없다.
- **[관찰]** 행사 종료가 종료 시각에 따라 자동인지 관리자의 명시적 조작인지 알 수 없다.
- **[관찰]** 한 카카오 계정이 팬과 크리에이터 역할을 동시에 가질 수 있는지 알 수 없다.
- **[관찰]** 예약번호 형식과 QR payload가 예약번호인지 상세 URL인지 알 수 없다.
- **[관찰]** 예약 취소·수령 완료·POS 취소가 재고에 미치는 구체적 순서와 실패 결과는 화면에 없다.
- **[관찰]** 이전 행사 상품 복사 시 이미지·설명·가격 중 무엇을 복사하고 무엇을 새로 입력하는지 알 수 없다.
- **[관찰]** 공지 고정 변경 시 기존 고정 공지를 자동 해제할지 확인 대화상자를 띄울지 알 수 없다.
- **[관찰]** 목록 데이터가 많아졌을 때 정렬·페이지 처리 방식을 알 수 없다.
- **[명세]** 고급 검색·필터와 성능 최적화는 제외한다.
- **[추정]** 첫 MVP에서는 작은 목록을 전제로 단순 전체 조회 또는 최소 페이지 처리만 고려한다.

## 5. 필요한 화면 상태

### 5.1 모든 API 연동 화면

- **[추정]** 최초 로딩, 정상 데이터, 데이터 없음, 요청 실패, 저장 중, 저장 성공, 저장 실패
- **[추정]** 권한 없음, 로그인 만료, 존재하지 않거나 삭제된 대상
- **[추정]** 삭제 확인, 취소 확인처럼 사용자가 데이터를 잃을 수 있는 동작의 최소 확인 상태

### 5.2 도메인별 상태

| 영역 | 필요한 상태 |
| --- | --- |
| 인증 | **[명세]** 비로그인, 로그인됨, 팬, 크리에이터, 지정 관리자 / **[추정]** OAuth 처리 중·실패 |
| 행사 | **[명세]** 비공개, 공개, 종료 / **[추정]** 폼 신규·수정·저장 중 |
| 참가 신청 | **[명세]** 신청 중, 승인, 반려 |
| 부스 | **[명세]** 기본 부스, 행사별 부스, 공개·비공개, 종료 후 읽기 전용 |
| 상품 | **[명세]** 공개·비공개, 예약 가능·불가, 유한·무한 재고, SOLD OUT 켜짐·꺼짐 |
| 이미지 | **[추정]** 미선택, 미리보기, 업로드 권한 요청 중, R2 업로드 중, 완료, 실패, 재선택 |
| 예약 | **[명세]** 예약, 수령 완료, 취소 / **[추정]** 생성 중, 예약번호·QR 발급 완료 |
| POS 판매 | **[명세]** 판매 기록, 취소 / **[추정]** 상품 미선택, 카트 구성, 저장 중, 저장 완료 |
| 공지 | **[명세]** 일반, 고정, 비고정 / **[추정]** 기존 고정 공지 교체 확인 |

### 5.3 종료 행사에서 별도로 보여야 할 상태

- **[명세]** 팬 공개 화면은 같은 URL에서 계속 조회할 수 있다.
- **[명세]** 신규 예약과 신규 POS 판매는 불가능하다.
- **[명세]** 기존 예약 취소는 가능하다.
- **[추정]** 공개 화면에는 “종료된 행사” 안내를 표시하고 예약 버튼을 숨기거나 비활성화한다.
- **[추정]** 크리에이터 관리 화면은 읽기 전용 안내와 함께 수정·신규 판매 버튼을 비활성화한다.

## 6. 예상 API와 데이터

아래는 확정 API 명세가 아니라 2단계 구현 설계를 위한 최소 후보이다.

### 6.1 예상 API

| 영역 | 예상 엔드포인트 | 근거 |
| --- | --- | --- |
| 인증 | `GET /api/auth/kakao/start`, `GET /api/auth/kakao/callback`, `GET /api/me`, `POST /api/logout` | **[명세]** 카카오 OAuth와 역할 라우팅 |
| 공개 행사 | `GET /api/events`, `GET /api/events/{eventId}` | **[명세]** 팬 행사 목록·상세 |
| 공개 부스 | `GET /api/events/{eventId}/booths`, `GET /api/event-booths/{eventBoothId}` | **[명세]** 행사별 승인·공개 부스 조회 |
| 공개 상품 | `GET /api/event-booths/{eventBoothId}/products`, `GET /api/event-products/{eventProductId}` | **[명세]** 부스 상품 목록·상세 |
| 관리자 행사 | `POST /api/admin/events`, `GET/PATCH/DELETE /api/admin/events/{eventId}`, `POST /api/admin/events/{eventId}/publish`, `POST /api/admin/events/{eventId}/end` | **[명세]** 행사 CRUD·공개·종료 |
| 참가 승인 | `GET /api/admin/event-applications`, `POST /api/admin/event-applications/{id}/approve`, `POST /api/admin/event-applications/{id}/reject` | **[명세]** 신청 목록·승인·반려 |
| 기본 부스 | `GET/POST /api/creator/booths`, `GET/PATCH/DELETE /api/creator/booths/{boothId}` | **[명세]** 크리에이터 부스 CRUD |
| 행사 참가 | `POST /api/creator/event-applications`, `GET /api/creator/event-applications` | **[명세]** 참가 신청·상태 조회 |
| 행사별 부스 | `GET/PATCH /api/creator/event-booths/{id}` | **[명세]** 승인 후 행사별 정보 관리 |
| 상품 | `GET/POST /api/creator/event-booths/{id}/products`, `GET/PATCH/DELETE /api/creator/event-products/{id}`, `POST /api/creator/event-booths/{id}/products/copy` | **[명세]** 상품 CRUD·이전 상품 복사 |
| 공지 | `GET/POST /api/creator/event-booths/{id}/notices`, `GET/PATCH/DELETE /api/creator/notices/{id}`, `POST /api/creator/notices/{id}/pin` | **[명세]** 공지 CRUD·한 개 고정 |
| 팬 예약 | `POST /api/reservations`, `GET /api/reservations`, `GET /api/reservations/{id}`, `POST /api/reservations/{id}/cancel` | **[명세]** 직접 예약·내 예약·취소 |
| 예약 운영 | `GET /api/creator/reservations`, `GET /api/creator/reservations/by-number/{no}`, `POST /api/creator/reservations/{id}/pickup` | **[명세]** 예약 조회·번호 검색·수령 완료 |
| POS | `POST /api/creator/pos-sales`, `GET /api/creator/pos-sales`, `GET /api/creator/pos-sales/{id}`, `POST /api/creator/pos-sales/{id}/cancel` | **[명세]** 간단 판매 CRUD 성격의 흐름 |
| R2 업로드 | `POST /api/uploads/presign` | **[명세]** 서버가 경로와 제한을 정한 직접 업로드 |

### 6.2 최소 데이터 후보

| 엔티티 | 최소 필드 |
| --- | --- |
| User | **[명세]** `id`, `kakaoSubject`, `role`, `displayName`, `createdAt` |
| Event | **[명세]** `id`, `name`, `startAt`, `endAt`, `venue`, `description`, `imageKey`, `reservationStartAt`, `reservationEndAt`, `status` |
| Booth | **[명세]** `id`, `ownerUserId`, `name`, `description`, `imageKey`, `snsUrl` |
| EventBooth | **[명세]** `id`, `eventId`, `boothId`, `boothNumber`, `intro`, `status`, `isPublic` |
| Product | **[명세]** `id`, `boothId`, `name`, `description`, `imageKey` |
| EventProduct | **[명세]** `id`, `eventBoothId`, `productId`, `price`, `stockMode`, `stockQuantity`, `isSoldOut`, `isPublic`, `reservationEnabled` |
| BoothNotice | **[명세]** `id`, `eventBoothId`, `title`, `body`, `isPinned`, `createdAt` |
| Reservation | **[명세]** `id`, `reservationNo`, `userId`, `eventBoothId`, `status`, `qrToken`, `createdAt`, `pickedUpAt`, `canceledAt` |
| ReservationItem | **[명세]** `id`, `reservationId`, `eventProductId`, `quantity`, `unitPrice` |
| PosSale | **[명세]** `id`, `eventBoothId`, `paymentMethod`, `status`, `soldAt` |
| PosSaleItem | **[명세]** `id`, `posSaleId`, `eventProductId`, `quantity`, `unitPrice` |

- **[추정]** `imageKey`는 DB에 R2 객체 키를 저장하고 공개 URL은 서버 또는 CDN 규칙으로 조합하는 방식이 적합하다.
- **[추정]** 예약 생성 요청은 한 `eventBoothId`와 여러 `{eventProductId, quantity}` 항목을 받는 형태가 승인 계획의 “한 부스 안에서 직접 예약”과 맞는다.
- **[추정]** 재고 이력 테이블, 결제 테이블, 통계용 집계 테이블은 만들지 않는다.

## 7. 공통 컴포넌트 후보

공통 후보는 재사용이 실제로 반복되는 최소 범위만 제안한다.

| 후보 | 사용 위치 | 근거 |
| --- | --- | --- |
| `PublicHeader`, `RoleHeader`, `SideNav`, `Footer` | 팬 공개 화면, 크리에이터·관리자 콘솔 | **[관찰]** 역할별로 동일 골격이 반복됨 |
| `PageHeader`, `SectionHeader` | 목록·상세·폼 전반 | **[관찰]** 제목·설명·주요 행동 배치가 반복됨 |
| `Button`, `StatusChip`, `Tabs` | 전 화면 | **[관찰]** 동일 시각 언어와 상태색 반복 |
| `FormField`, `TextInput`, `Select`, `Textarea` | 행사·부스·상품·공지 폼 | **[관찰]** 입력 패턴 반복 |
| `ImageUploader` | 행사·부스·상품 | **[명세]** R2 업로드 흐름 공유 필요 |
| `DataTable`, `DetailList` | 관리자·크리에이터 목록/상세 | **[관찰]** 표와 key-value 상세 패턴 반복 |
| `ConfirmDialog`, `Toast` | 삭제·취소·저장 | **[추정]** 최소 안전 확인과 결과 피드백 필요 |
| `LoadingState`, `EmptyState`, `ErrorState` | API 연결 화면 | **[추정]** 정적 화면에 빠져 있는 필수 실행 상태 |
| `EventCard`, `BoothCard`, `ProductCard` | 팬 목록·관련 항목 | **[관찰]** 같은 도메인 카드가 여러 화면에서 반복됨 |
| `ReservationStatus`, `StockDisplay` | 팬·크리에이터·POS | **[명세]** 예약 상태와 유한·무한·SOLD OUT 표시 규칙을 한곳에서 일관되게 적용해야 함 |

## 8. 화면별 고유 컴포넌트

### 8.1 팬

- **[추정]** `EventHero`: 행사 일정·장소·종료 상태
- **[추정]** `EventBoothList`: 해당 행사에 승인·공개된 부스 표시
- **[추정]** `BoothProfileHero`: 부스 이미지·소개·부스 번호·SNS
- **[추정]** `PinnedBoothNotice`: 상단 고정 공지 한 개와 일반 공지 목록 진입
- **[추정]** `ProductDetailPanel`: 가격·예약 가능·재고·SOLD OUT 표시
- **[추정]** `DirectReservationForm`: 한 부스 내 상품·수량 선택과 예약 제출
- **[추정]** `ReservationTicket`: 예약번호, QR, 행사·부스·상품·상태, 취소 동작

### 8.2 크리에이터

- **[추정]** `EventApplicationForm`: 기본 부스를 선택해 참가 신청
- **[추정]** `EventBoothEditor`: 행사별 소개·부스 번호·공개 정보 편집
- **[추정]** `PreviousProductCopyPanel`: 과거 행사와 상품을 선택해 복사
- **[추정]** `StockModeEditor`: 유한/무한, 수량, 예약 가능, 수동 SOLD OUT
- **[추정]** `ReservationLookupPanel`: 예약번호 검색과 수령 완료
- **[추정]** `PosProductPicker`, `PosDraft`, `PosSaleConfirm`: 단순 POS 판매 기록 흐름
- **[추정]** `BoothNoticeEditor`: 공지 CRUD와 고정 공지 교체

### 8.3 관리자

- **[추정]** `EventEditor`: 행사 CRUD·공개·종료
- **[추정]** `ParticipationReviewTable`: 행사 참가 신청 목록
- **[추정]** `ParticipationReviewPanel`: 신청 상세, 승인·반려와 최소 반려 사유

## 9. 구현 전 확인이 필요한 사항

### 9.1 데이터 구조 또는 핵심 흐름에 영향을 주므로 확정이 필요한 사항

1. **한 카카오 계정의 역할 수**
   - **[관찰]** 프로토타입은 로그인 전에 팬/크리에이터 유형을 선택한다.
   - **[명세]** 최소 데이터는 `User.role` 단일 값으로 표현되어 있다.
   - **[추정]** “계정 하나당 팬 또는 크리에이터 한 역할, 지정 계정은 관리자”로 구현 가능하다.
   - **확정 필요:** 한 카카오 계정이 팬과 크리에이터를 동시에 사용할 수 있어야 하는가. 동시 역할이면 사용자·권한 구조가 달라진다.

2. **연결 데이터가 있는 항목의 삭제 규칙**
   - **[명세]** 행사·부스·상품 CRUD에는 삭제가 포함된다.
   - **[관찰]** 프로토타입은 제거·비공개 버튼을 보여주지만 연결 예약·판매가 있을 때의 결과는 없다.
   - **[추정]** 예약·POS가 연결된 행사/부스/상품은 삭제를 막고 비공개·종료로 전환하는 편이 데이터 손실을 줄인다.
   - **확정 필요:** 연결 기록이 있어도 물리 삭제할지, 삭제를 막을지.

3. **행사 종료 판정 방식**
   - **[명세]** 관리자가 행사를 종료하고 종료 후 읽기 전용이 된다.
   - **[관찰]** 화면에는 종료일과 상태 선택이 모두 있다.
   - **[추정]** MVP에서는 관리자 수동 종료를 기준으로 하고 종료일은 표시·검증용으로 쓰는 방식이 가장 단순하다.
   - **확정 필요:** 종료 시각 도달 시 자동 종료가 반드시 필요한가.

4. **이전 상품 복사 범위**
   - **[명세]** 상품 정보 복사와 행사별 가격·재고 재설정이 포함된다.
   - **[명세-초안]** 과거 재고·판매·주문 기록은 복사하지 않는다.
   - **[추정]** 상품명·설명·이미지를 복사하고 가격·재고·공개·예약·SOLD OUT은 새 행사 값으로 입력한다.
   - **확정 필요:** 이전 가격도 초깃값으로 복사할지.

### 9.2 2단계 구현 설계에서 결정해도 되는 사항

- **[추정]** 서비스 표기명을 프로토타입의 `BoothUp`으로 유지할지 저장소명에 맞춰 `BoothHana2` 또는 별도 한글명으로 바꿀지
- **[추정]** 정확한 React URL과 프로토타입 화면 병합 방식
- **[추정]** 예약 QR에 예약번호만 넣을지 예약 상세 URL을 넣을지
- **[추정]** 이미지 허용 형식, 최대 크기, 이미지 수와 미리보기 방식
- **[추정]** 공지 고정 교체 시 확인창 문구와 일반 공지 노출 위치
- **[추정]** 모바일 우선 해상도와 표의 모바일 표현
- **[추정]** 로딩 스켈레톤·빈 상태·오류 문구의 구체적 모양

위 항목은 쉽게 되돌릴 수 있는 UI 세부사항이거나 승인 계획에서 개발자 자율 결정으로 둔 부분이므로, 별도 요구가 없다면 2단계에서 가장 단순한 안을 제시할 수 있다.

## 1단계 결론과 2단계 전달 내용

- **[명세]** 구현 범위의 계약은 승인된 Lean CRUD 계획이며 프로토타입의 73개 HTML 전체가 아니다.
- **[추정]** 팬은 행사 → 부스 → 상품 → 직접 예약 → 예약표 확인 흐름을 중심으로 재구성한다.
- **[추정]** 크리에이터는 행사 참가 → 행사 부스 → 상품·재고·공지 → 예약 수령/POS 흐름을 중심으로 재구성한다.
- **[추정]** 관리자는 행사와 참가 승인만 남긴다.
- **[추정]** 2단계에서는 위 포함 화면의 React 라우트, 화면 상태표, API 요청·응답, DB 관계, 첫 구현 단위를 확정한다.
- **[명세]** 3단계 구현은 2단계 설계가 확인된 뒤 `$build-lean-crud`로 수행한다.

